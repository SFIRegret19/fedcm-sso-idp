version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "443:443"
    environment:
      - DATABASE_URL=sqlite:///./sso_database.db
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"